`1.0.0`
-------

- Enable Mercado Pago Checkout Pro installments on website payments. The
  native provider forced a single installment; the maximum is now
  configurable (default 12) and the chosen plan is stored on the
  transaction.
